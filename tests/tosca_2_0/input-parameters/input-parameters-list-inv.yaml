tosca_definitions_version: tosca_2_0

service_template:

  inputs:
    - foo-name: # Invalid: 'inputs' must be a map of parameter definitions, not a list
        type: string
        description: This is intentionally invalid for a negative test.
        default: bar



