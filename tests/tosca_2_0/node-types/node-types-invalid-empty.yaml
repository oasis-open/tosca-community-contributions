tosca_definitions_version: tosca_2_0
capability_types:
  TransactSQL:
    description: >
      This capability indicates an entity that supports SQL
node_types: # Invalid: node_types should not be present and empty

group_types:
  MyGroupType:
    description: A sample group type
    properties:
      my-property:
        type: string
        default: null

