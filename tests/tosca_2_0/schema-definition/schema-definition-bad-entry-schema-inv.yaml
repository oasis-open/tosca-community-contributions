tosca_definitions_version: tosca_2_0

node_types:
  SchemaTester:
    properties:
      bad:
        type: list
        entry_schema:
          type: map
          entry_schema:
            type: NonExistentType

service_template:
  node_templates:
    tester1:
      type: SchemaTester
      properties:
        bad:
          - { user1: [ "a" ] }

