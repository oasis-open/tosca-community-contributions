tosca_definitions_version: tosca_2_0



node_types:
  SchemaTester:
    properties:
      invalid:
        type: map
        # Invalid as entry_schema for map must be a string type
        entry_schema: integer
        

