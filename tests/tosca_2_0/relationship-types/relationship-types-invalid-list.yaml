tosca_definitions_version: tosca_2_0

relationship_types:
  - HostedOn: # Invalid: should be a map, not a list
      properties:
        property-1:
          type: string
        # more details ...

  - MyCustomClientServer:
      derived_from: HostedOn
      properties:
        another-property:
          type: string
        # more details ...

  - MyCustomConnectionType:
      properties:
        connection-type:
          type: string
        # more details ...
