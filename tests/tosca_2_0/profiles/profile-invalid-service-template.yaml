tosca_definitions_version: tosca_2_0
node_types:
  MyNodeType:
    properties:
      my_property:
        type: string
        description: A sample property for MyNodeType.

profile: com.example.tosca_profiles.cloud_computing:2.0 
# Invalid as file includes both a profile and a service_template
service_template:
  description: A service template with an invalid profile reference.
  node_templates:
    my_node:
      type: MyNodeType

