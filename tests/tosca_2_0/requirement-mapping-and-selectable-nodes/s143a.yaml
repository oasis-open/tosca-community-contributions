# tag::s143[]
tosca_definitions_version: tosca_2_0

imports:
- requirement-mapping-and-selectable-nodes-types.yaml

service_template:
  node_templates:
    compute:
      type: Compute
    client:
      type: ClientSoftware
      directives: [ substitute ]
      requirements:
      - host: compute
# end::s143[]
