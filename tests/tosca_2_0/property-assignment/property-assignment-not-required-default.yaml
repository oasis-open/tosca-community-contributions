
tosca_definitions_version: tosca_2_0

capability_types:
  Endpoint:
    properties:
      protocol:
        type: string
        default: tcp



node_types:
  Host:
    capabilities:
      terminal:
        type: Endpoint

service_template:
  node_templates:
    my_host:
      type: Host


# needs a stage 2 test to verify behavior