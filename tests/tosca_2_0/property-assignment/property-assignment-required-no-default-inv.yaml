# Error: Required property has no value defined, assigned, or default value
tosca_definitions_version: tosca_2_0

capability_types:
  Endpoint:
    properties:
      protocol:
        type: string
        required: true


node_types:
  Host:
    capabilities:
      terminal:
        type: Endpoint

service_template:
  node_templates:
    my_host:
      type: Host

