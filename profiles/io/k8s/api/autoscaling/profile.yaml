tosca_definitions_version: tosca_simple_yaml_1_3

# This template was auto-generated by swagger2tosca

namespace: io.k8s.api.autoscaling

metadata:
  title: Kubernetes
  version: unversioned

imports:
  - file: io.k8s.apimachinery.pkg.apis.meta
    namespace_prefix: meta

node_types:

  Scale:
    description: Scale represents a scaling request for a resource.
    derived_from: tosca.nodes.Root
    metadata:
      x-kubernetes-group-version-kind:
        group: autoscaling
        kind: Scale
        version: v1
    properties:
      apiVersion:
        description: >-
          APIVersion defines the versioned schema of this representation of an
          object. Servers should convert recognized schemas to the latest
          internal value, and may reject unrecognized values. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#resources
        required: false
        type: string
      kind:
        description: >-
          Kind is a string value representing the REST resource this object
          represents. Servers may infer this from the endpoint the client
          submits requests to. Cannot be updated. In CamelCase. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#types-kinds
        required: false
        type: string
      metadata:
        description: >-
          Standard object metadata; More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#metadata.
        required: false
        type: meta:ObjectMeta
      spec:
        description: >-
          spec defines the behavior of the scale. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#spec-and-status.
        required: false
        type: ScaleSpec
      status:
        description: >-
          status is the current status of the scale. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#spec-and-status. Read-only.
        required: false
        type: ScaleStatus

  HorizontalPodAutoscaler:
    description: configuration of a horizontal pod autoscaler.
    derived_from: tosca.nodes.Root
    metadata:
      x-kubernetes-group-version-kind:
        group: autoscaling
        kind: HorizontalPodAutoscaler
        version: v1
    properties:
      apiVersion:
        description: >-
          APIVersion defines the versioned schema of this representation of an
          object. Servers should convert recognized schemas to the latest
          internal value, and may reject unrecognized values. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#resources
        required: false
        type: string
      kind:
        description: >-
          Kind is a string value representing the REST resource this object
          represents. Servers may infer this from the endpoint the client
          submits requests to. Cannot be updated. In CamelCase. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#types-kinds
        required: false
        type: string
      metadata:
        description: >-
          Standard object metadata. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#metadata
        required: false
        type: meta:ObjectMeta
      spec:
        description: >-
          spec defines the behaviour of autoscaler. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#spec-and-status.
        required: false
        type: HorizontalPodAutoscalerSpec
      status:
        description: status is the current information about the autoscaler.
        required: false
        type: HorizontalPodAutoscalerStatus

data_types:

  ScaleSpec:
    description: ScaleSpec describes the attributes of a scale subresource.
    properties:
      replicas:
        description: replicas is the desired number of instances for the scaled object.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]

  CrossVersionObjectReference:
    description: >-
      CrossVersionObjectReference contains enough information to let you
      identify the referred resource.
    properties:
      apiVersion:
        description: apiVersion is the API version of the referent
        required: false
        type: string
      kind:
        description: >-
          kind is the kind of the referent; More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#types-kinds
        required: true
        type: string
      name:
        description: >-
          name is the name of the referent; More info:
          https://kubernetes.io/docs/concepts/overview/working-with-
          objects/names/#names
        required: true
        type: string

  HorizontalPodAutoscalerSpec:
    description: specification of a horizontal pod autoscaler.
    properties:
      maxReplicas:
        description: >-
          maxReplicas is the upper limit for the number of pods that can be set
          by the autoscaler; cannot be smaller than MinReplicas.
        required: true
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      minReplicas:
        description: >-
          minReplicas is the lower limit for the number of replicas to which the
          autoscaler can scale down.  It defaults to 1 pod.  minReplicas is
          allowed to be 0 if the alpha feature gate HPAScaleToZero is enabled
          and at least one Object or External metric is configured.  Scaling is
          active as long as at least one metric value is available.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      scaleTargetRef:
        description: >-
          reference to scaled resource; horizontal pod autoscaler will learn the
          current resource consumption and will set the desired number of pods
          by using its Scale subresource.
        required: true
        type: CrossVersionObjectReference
      targetCPUUtilizationPercentage:
        description: >-
          targetCPUUtilizationPercentage is the target average CPU utilization
          (represented as a percentage of requested CPU) over all the pods; if
          not specified the default autoscaling policy will be used.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]

  ScaleStatus:
    description: ScaleStatus represents the current status of a scale subresource.
    properties:
      replicas:
        description: >-
          replicas is the actual number of observed instances of the scaled
          object.
        required: true
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      selector:
        description: >-
          selector is the label query over pods that should match the replicas
          count. This is same as the label selector but in the string format to
          avoid introspection by clients. The string will be in the same format
          as the query-param syntax. More info about label selectors:
          https://kubernetes.io/docs/concepts/overview/working-with-
          objects/labels/
        required: false
        type: string

  HorizontalPodAutoscalerStatus:
    description: current status of a horizontal pod autoscaler
    properties:
      currentCPUUtilizationPercentage:
        description: >-
          currentCPUUtilizationPercentage is the current average CPU utilization
          over all pods, represented as a percentage of requested CPU, e.g. 70
          means that an average pod is using now 70% of its requested CPU.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      currentReplicas:
        description: >-
          currentReplicas is the current number of replicas of pods managed by
          this autoscaler.
        required: true
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      desiredReplicas:
        description: >-
          desiredReplicas is the  desired number of replicas of pods managed by
          this autoscaler.
        required: true
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      lastScaleTime:
        description: >-
          lastScaleTime is the last time the HorizontalPodAutoscaler scaled the
          number of pods; used by the autoscaler to control how often the number
          of pods is changed.
        required: false
        type: meta:Time
      observedGeneration:
        description: >-
          observedGeneration is the most recent generation observed by this
          autoscaler.
        required: false
        type: integer
        constraints:
          - in_range: [-9223372036854775808, 9223372036854775807]

