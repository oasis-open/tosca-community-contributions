tosca_definitions_version: tosca_simple_yaml_1_3

# This template was auto-generated by swagger2tosca

namespace: io.k8s.api.coordination

metadata:
  title: Kubernetes
  version: unversioned

imports:
  - file: io.k8s.apimachinery.pkg.apis.meta
    namespace_prefix: meta

node_types:

  Lease:
    description: Lease defines a lease concept.
    derived_from: tosca.nodes.Root
    metadata:
      x-kubernetes-group-version-kind:
        group: coordination.k8s.io
        kind: Lease
        version: v1
    properties:
      apiVersion:
        description: >-
          APIVersion defines the versioned schema of this representation of an
          object. Servers should convert recognized schemas to the latest
          internal value, and may reject unrecognized values. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#resources
        required: false
        type: string
      kind:
        description: >-
          Kind is a string value representing the REST resource this object
          represents. Servers may infer this from the endpoint the client
          submits requests to. Cannot be updated. In CamelCase. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#types-kinds
        required: false
        type: string
      metadata:
        description: >-
          More info: https://git.k8s.io/community/contributors/devel/sig-
          architecture/api-conventions.md#metadata
        required: false
        type: meta:ObjectMeta
      spec:
        description: >-
          spec contains the specification of the Lease. More info:
          https://git.k8s.io/community/contributors/devel/sig-architecture/api-
          conventions.md#spec-and-status
        required: false
        type: LeaseSpec

data_types:

  LeaseSpec:
    description: LeaseSpec is a specification of a Lease.
    properties:
      acquireTime:
        description: acquireTime is a time when the current lease was acquired.
        required: false
        type: meta:MicroTime
      holderIdentity:
        description: >-
          holderIdentity contains the identity of the holder of a current lease.
          If Coordinated Leader Election is used, the holder identity must be
          equal to the elected LeaseCandidate.metadata.name field.
        required: false
        type: string
      leaseDurationSeconds:
        description: >-
          leaseDurationSeconds is a duration that candidates for a lease need to
          wait to force acquire it. This is measured against the time of last
          observed renewTime.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      leaseTransitions:
        description: >-
          leaseTransitions is the number of transitions of a lease between
          holders.
        required: false
        type: integer
        constraints:
          - in_range: [-2147483648, 2147483647]
      preferredHolder:
        description: >-
          PreferredHolder signals to a lease holder that the lease has a more
          optimal holder and should be given up. This field can only be set if
          Strategy is also set.
        required: false
        type: string
      renewTime:
        description: >-
          renewTime is a time when the current holder of a lease has last
          updated the lease.
        required: false
        type: meta:MicroTime
      strategy:
        description: >-
          Strategy indicates the strategy for picking the leader for coordinated
          leader election. If the field is not specified, there is no active
          coordination for this lease. (Alpha) Using this field requires the
          CoordinatedLeaderElection feature gate to be enabled.
        required: false
        type: string

